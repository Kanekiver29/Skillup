@extends('sias.admin.layouts.master')
@section('title', 'Student Status')
@section('page_title', 'Student Status')
@section('subtitle', 'Monitor learner account, registration, and enrollment status at a glance.')
@section('content')
<div class="admin-card" style="overflow:hidden;"><div style="margin-bottom:1rem;"><h3 style="margin:0;">Learner status register</h3><p style="margin:.3rem 0 0;color:var(--text-muted);">{{ $students->count() }} learners currently registered.</p></div><div style="overflow-x:auto;"><table style="width:100%;min-width:680px;border-collapse:collapse;"><thead><tr style="text-align:left;color:var(--text-muted);font-size:.75rem;text-transform:uppercase;border-bottom:1px solid var(--card-border);"><th style="padding:.75rem;">Learner</th><th style="padding:.75rem;">Account</th><th style="padding:.75rem;">Registration</th><th style="padding:.75rem;">Enrollment</th><th style="padding:.75rem;">Action</th></tr></thead><tbody>@forelse($students as $student)@php($data = $student->settings['registration'] ?? [])<tr style="border-bottom:1px solid var(--card-border);"><td style="padding:.75rem;"><strong>{{ $student->name }}</strong><small style="display:block;color:var(--text-muted);">{{ $student->lrn ?? $student->id }}</small></td><td style="padding:.75rem;"><span style="color:#15803d;">Active</span><small style="display:block;color:var(--text-muted);">{{ $student->email }}</small></td><td style="padding:.75rem;">{{ $data['entry_date'] ?? '—' }}</td><td style="padding:.75rem;">{{ $data['training_program'] ?? 'Not assigned' }}</td><td style="padding:.75rem;"><a href="{{ route('sias.admin.students.show', $student->id) }}" style="color:var(--accent);">View</a></td></tr>@empty<tr><td colspan="5" style="padding:2rem;text-align:center;color:var(--text-muted);">No students found.</td></tr>@endforelse</tbody></table></div></div>
@endsection
