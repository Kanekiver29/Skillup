@extends('sias.teacher.layout.layout')
@section('title', 'Edit Training Schedule')
@section('page_title', 'Edit Training Schedule')
@section('content')
<div class="page-card"><h2>Edit Training Schedule</h2><form method="POST" action="{{ route('sias.teacher.schedule.update', $schedule) }}" style="display:grid;gap:14px;max-width:720px">@csrf @method('PUT')<label>Program<select name="course_id"><option value="">Select program</option>@foreach($courses as $course)<option value="{{ $course->id }}" @selected($schedule->course_id == $course->id)>{{ $course->title }}</option>@endforeach</select></label><label>Subject / activity<input name="subject_name" value="{{ $schedule->subject_name }}" required></label><label>Day<select name="day_of_week" required>@foreach(['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'] as $day)<option @selected($schedule->day_of_week === $day)>{{ $day }}</option>@endforeach</select></label><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px"><label>Start time<input type="time" name="start_time" value="{{ substr($schedule->start_time, 0, 5) }}" required></label><label>End time<input type="time" name="end_time" value="{{ substr($schedule->end_time, 0, 5) }}" required></label></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px"><label>Building<input name="building" value="{{ $schedule->building }}"></label><label>Room<input name="room_number" value="{{ $schedule->room_number }}"></label></div><label>Trainee count<input type="number" name="student_count" min="0" value="{{ $schedule->student_count }}"></label><label>Notes<textarea name="notes">{{ $schedule->notes }}</textarea></label><button class="btn" type="submit">Update schedule</button></form></div>
@endsection