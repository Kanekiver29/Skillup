@extends('sias.teacher.layout.layout')
@section('title', 'Attendance')
@section('page_title', 'Attendance')
@section('content')
<div class="page-card"><div style="display:flex;justify-content:space-between;gap:1rem;align-items:center;flex-wrap:wrap"><div><h2>Daily Attendance</h2><p>Record Present, Absent, or Late attendance for {{ $date }}.</p></div><a class="btn" href="{{ route('sias.teacher.reports') }}">Attendance report</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('sias.teacher.attendance.store') }}">@csrf<div style="display:flex;gap:1rem;align-items:end;flex-wrap:wrap;margin:1rem 0"><label>Date<input type="date" name="attendance_date" value="{{ $date }}" required></label><button class="btn" type="submit">Save attendance</button></div><div style="overflow:auto"><table class="portal-table"><thead><tr><th>Trainee</th><th>Program</th><th>Status</th><th>Notes</th></tr></thead><tbody>@forelse($enrollments as $enrollment)@php($record = $records->get($enrollment->id))<tr><td>{{ $enrollment->user?->name ?? '—' }}</td><td>{{ $enrollment->course?->title ?? '—' }}</td><td><select name="attendance[{{ $enrollment->id }}]"><option value="present" @selected(($record?->status ?? 'present') === 'present')>Present</option><option value="late" @selected($record?->status === 'late')>Late</option><option value="absent" @selected($record?->status === 'absent')>Absent</option></select></td><td><input name="notes[{{ $enrollment->id }}]" value="{{ $record?->notes }}" placeholder="Optional note"></td></tr>@empty<tr><td colspan="4">No trainees available.</td></tr>@endforelse</tbody></table></div></form><div class="portal-grid"><div class="portal-card"><strong>{{ $summary->get('present', 0) }}</strong><span>Present</span></div><div class="portal-card"><strong>{{ $summary->get('late', 0) }}</strong><span>Late</span></div><div class="portal-card"><strong>{{ $summary->get('absent', 0) }}</strong><span>Absent</span></div></div></div>
@endsection